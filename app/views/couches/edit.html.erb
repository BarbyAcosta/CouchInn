<div class="container-fluid">
	<div class="row">
    <div class="col-lg-10">

	<div class="jumbotron"> 
		<label for=“name"> <font face="arial" size="5" color="green"> Ingrese los datos a modificar </font> </label>
 
 		<p  


		/p>


		<%= form_for(@couch) do |f| %> 
			<% if @couch.errors.any? %>
  				<div id="error_explanation">
    				<h2><%= pluralize(@couch.errors.count, "error") %> No se han guardado los cambios:</h2>
    				<ul>
    					<% @couch.errors.full_messages.each do |msg| %>
      						<li><%= msg %></li>
    					<% end %>
    				</ul>
 				 </div>
  			<% end %>
			
			<p>
				<%= f.label :name, 'Nombre del couch' %>
				<%= f.text_field :name %>
			</p>
			
			<p>
				<%= f.label :description, 'Descripcion del couch' %>
				<%= f.text_field :description %>
			</p>

			<p>
				<%= f.label :localidad, 'Localidad del couch' %>
				<%= f.text_field :localidad %>
			</p>
		
            <p>
				<%= f.label :capacidad, 'Capacidad del couch' %>
				<%= f.text_field :capacidad %>
			</p>

		<%= f.submit 'Guardar' %>

    	<% end %> 

		</p> 
 

		<p>

			<p> <%= link_to "Volver", href="/welcome/index" %> </p>

		</p> 


	</div>
	
</div> 